apiVersion: v1
kind: Service
metadata:
  name: api-gateway-private
  namespace: stashfi
  labels:
    app: api-gateway
spec:
  type: ClusterIP
  selector:
    app: api-gateway
  ports:
    - name: http-private
      port: 8081
      targetPort: 8081
      protocol: TCP
