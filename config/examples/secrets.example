# Copy to .secrets (do NOT commit .secrets)
# GitHub Container Registry token for pushing images
GHCR_TOKEN=ghp_xxx

# NPM token for private packages (if needed)
NPM_TOKEN=xxx

# Database credentials for integration tests
POSTGRES_PASSWORD=postgres

# Redis password (if needed)
REDIS_PASSWORD=

# API keys for external services (add as needed)
# SLACK_WEBHOOK_URL=
# DISCORD_WEBHOOK_URL=

# Cloud credentials (only if testing cloud deployments locally)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# GCP_SERVICE_ACCOUNT_KEY=
